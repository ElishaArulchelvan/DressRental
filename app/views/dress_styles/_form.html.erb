<%= form_for(@dress_style) do |f| %>
  <% if @dress_style.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dress_style.errors.count, "error") %> prohibited this dress_style from being saved:</h2>

      <ul>
      <% @dress_style.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :style %><br>
    <%= f.text_field :style %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
